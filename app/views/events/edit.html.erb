<h1>イベントの編集</h1>

<div class="panel panel-default">
  <div class="panel-body">
    <%= form_for @event_form, url: @event,
      html: { role: 'form', class: 'form-horizontal' } do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>
